import React, { useEffect, useState } from 'react';

import IconsView from '../view/Icons';

const ColorsTable: React.FC<any> = (): JSX.Element => {
	/**
	 * 로직 구현 없음
	 */

	return <IconsView />;
};

export default ColorsTable;
